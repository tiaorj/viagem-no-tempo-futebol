import type { Episode } from "@/types/episode"; import { TrackedLink } from "./tracked-link";
export function EpisodeCard({ episode, index }: { episode: Episode; index: number }) { const link = episode.links[0]; return <article className="group border border-white/10 bg-white/[.03] transition duration-300 hover:-translate-y-1 hover:border-gold/60"><div className="relative flex aspect-[16/9] items-end overflow-hidden bg-[radial-gradient(circle_at_70%_20%,#9d1b2344,transparent_45%),linear-gradient(135deg,#211717,#090909)] p-5"><span className="font-display text-6xl text-paper/10">{String(episode.episodeNumber ?? index + 1).padStart(2,"0")}</span><span className="absolute right-4 top-4 text-xs uppercase tracking-widest text-gold">{episode.historicalPeriod}</span></div><div className="p-5"><h3 className="font-display text-2xl text-paper">{episode.title}</h3><p className="mt-2 text-sm leading-6 text-paper/60">{episode.description}</p>{link ? <TrackedLink href={link.url} target="_blank" rel="noreferrer" event="episode_click" params={{episode_id:episode.id,episode_title:episode.title,series:episode.series,platform:link.platform,position:index+1}} className="mt-5 inline-block text-xs uppercase tracking-widest text-gold">Assistir à história ↗</TrackedLink> : <span className="mt-5 inline-block text-xs uppercase tracking-widest text-paper/30">Em breve</span>}</div></article> }
